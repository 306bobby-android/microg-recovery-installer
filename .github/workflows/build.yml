name: Build and release

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Weekly, so a new microG or Aurora Store release turns into a new zip.
    - cron: '17 6 * * 1'
  workflow_dispatch:
    inputs:
      release:
        description: 'Publish a GitHub release'
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check the shell scripts
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq shellcheck
          shellcheck -s sh zip/META-INF/com/google/android/update-binary \
                           zip/installer/util.sh zip/installer/main.sh
          shellcheck -s bash scripts/build_zip.sh

      - name: Download and verify the APKs
        run: python3 scripts/fetch_apks.py

      - name: Work out the version
        id: meta
        run: |
          set -a; . build/versions.env; set +a
          # The tag is derived from what actually goes into the zip: the upstream
          # component versions plus the installer skeleton. A README-only push
          # therefore does not cut a release, but a new microG (or a change to
          # the installer) does.
          stamp="$(
            { cut -d'|' -f8 build/apps.list
              git rev-parse 'HEAD:zip'
              git hash-object .env
            } | sha256sum | cut -c1-7
          )"
          version="${GMS_VERSION}-${stamp}"
          {
            echo "version=${version}"
            echo "tag=v${version}"
          } >> "$GITHUB_OUTPUT"
          {
            echo "microG **${GMS_VERSION}**, built $(date -u '+%Y-%m-%d')."
            echo
            echo '| Component | Package | Version |'
            echo '| --- | --- | --- |'
            while IFS='|' read -r _ name _ _ pkg _ _ ver; do
              [ -n "$name" ] && echo "| ${name} | \`${pkg}\` | ${ver} |"
            done < build/apps.list
            echo
            echo 'Flash in a custom recovery. Volume Up = yes, Volume Down = no.'
          } > build/notes.md

      - name: Build the zip
        run: scripts/build_zip.sh "${{ steps.meta.outputs.version }}"

      - uses: actions/upload-artifact@v4
        with:
          name: microg-recovery-installer-${{ steps.meta.outputs.version }}
          path: |
            dist/*.zip
            dist/*.sha256

      - name: Publish the release
        if: github.event_name != 'pull_request' && (github.event_name != 'workflow_dispatch' || inputs.release)
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.meta.outputs.tag }}
        run: |
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG already exists; nothing new upstream."
            exit 0
          fi
          gh release create "$TAG" \
            --title "$TAG" \
            --notes-file build/notes.md \
            dist/*.zip dist/*.sha256
